ActiveMQ是Apache出品的一个消息系统，并且是一个完全支持JMS规范的JMS Provider实现。

消息队列的应用场景  
消息队列的主要特点是异步通信，这样可以做到减少请求响应时间和解耦。主要的使用场景就是将比较耗时而且不需要同步返回结果的操作作为消息放入消息队列。同时消息发送方和接收方不需要彼此联系，不受对方影响，可以做到解耦。

主要的一些使用场景
- 跨系统的异步通信
- 提供多个应用之间的松耦合接口
- 应用内部的同步变异步
